import {
  StyleSheet,
} from 'react-native';
import colors from './colors';
const map = {
  mapContainer: {
    ...StyleSheet.absoluteFillObject,
    height: 400,
    width: 400,
    marginTop: 40,
    justifyContent: 'flex-end',
    alignItems: 'center',
  },
  map: {
    ...StyleSheet.absoluteFillObject,
  },
}

export default map;
